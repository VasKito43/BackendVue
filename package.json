{
  "name": "meu-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "postinstall": "npm run bootstrap",
    "bootstrap": "npm --workspace backend install && npm --workspace frontend install",
    "dev": "concurrently \"npm --workspace backend run dev\" \"npm --workspace frontend run dev\""
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
